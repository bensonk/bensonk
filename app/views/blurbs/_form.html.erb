<%= stylesheet_link_tag "scaffold" %>
<%= form_for(@blurb) do |f| %>
  <% if @blurb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blurb.errors.count, "error") %> prohibited this blurb from being saved:</h2>

      <ul>
      <% @blurb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :placeholder => "An Awesome Title" %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :placeholder => "A Body of Great Eloquence" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
